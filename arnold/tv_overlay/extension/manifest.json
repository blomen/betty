{
  "manifest_version": 3,
  "name": "Arnold TradingView Overlay",
  "version": "0.4.0",
  "description": "5-step heatmap zone palette (slate→indigo→fuchsia→orange→red) matching CLAUDE.md, paint threshold hides hierarchy<0.5 single-family zones, rectangle fallback when long_position widget creation fails (so trades stay visible even when TV refuses the native widget), promise-rejection labels surface in /api/tv-overlay/debug error field. Earlier: trailed stop separate dashed-green horizontal_line when stop crosses into profit, pyramid size in header, halt amber recolor, exit_reason suffix.",
  "host_permissions": [
    "ws://127.0.0.1:*/*",
    "ws://localhost:*/*",
    "http://127.0.0.1:*/*",
    "http://localhost:*/*",
    "https://*.tradingview.com/*"
  ],
  "content_scripts": [
    {
      "matches": ["https://*.tradingview.com/*"],
      "js": ["bridge.js"],
      "world": "ISOLATED",
      "run_at": "document_idle"
    },
    {
      "matches": ["https://*.tradingview.com/*"],
      "js": ["page.js"],
      "world": "MAIN",
      "run_at": "document_idle"
    }
  ]
}
